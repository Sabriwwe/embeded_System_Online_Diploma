/*
name : sabri talaat elenani .
Ex1 : c program to store all information (name, roll and marks) of student using structure
*/

#include <stdio.h>

// Define a structure to represent a student
struct Student {
    char name[50];
    int rollNumber;
    float marks;
};

int main() {
    struct Student student;

    // Input information of the student
     printf("Enter information of students:\n");
  
    printf("Enter name: ");
    scanf("%s", student.name);

    printf("Enter roll number: ");
    scanf("%d", &student.rollNumber);

    printf("Enter marks: ");
    scanf("%f", &student.marks);

    // Print the information of the student
    printf("\nIdisplaing informaion:\n");
    printf("Name: %s\n", student.name);
    printf("Roll Number: %d\n", student.rollNumber);
    printf("Marks: %.2f\n", student.marks);

    return 0;
}
