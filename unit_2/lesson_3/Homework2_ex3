/*

name : sabri talaat elenani.
Ex3 : c program to find largest number among three numbers 

*/

#include <stdio.h>

int main() {
    float num1, num2, num3;

    // Input three numbers from the user
    printf("Enter three numbers: ");
    scanf("%f %f %f", &num1, &num2, &num3);

    // Assume num1 as the largest
    float largest = num1;

    // Check if num2 is larger than the current largest
    if (num2 > largest) {
        largest = num2;
    }

    // Check if num3 is larger than the current largest
    if (num3 > largest) {
        largest = num3;
    }

    // Print the largest number
    printf("The largest number is: %f\n", largest);

    return 0;
}
